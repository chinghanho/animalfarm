(() => {

    'use strict'

    class Item extends Entity {

        constructor(options) {
            super(options)
        }

        animate(key, speed) {
            super.animate(key, speed)
        }

    }

    this.Item = Item

})()
