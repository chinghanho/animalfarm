(() => {

    'use strict'

    class Item extends Entity {

        constructor() {
            super()
        }

        animate(key, speed) {
            super.animate(key, speed)
        }

    }

    this.Item = Item

})()
